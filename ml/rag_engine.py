# LangChain RAG pipeline (retrieval + generation)

from langchain.chains import RetrievalQA

def get_rag_response(query):
    # TODO: Implement retriever + LLM chain
    return f"Mock RAG response for query: {query}"
